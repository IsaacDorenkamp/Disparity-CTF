html, body{
	margin: 0;
	padding: 0;
	height: 100%;
}

body{
	font-family: "Lato", "Arial", sans-serif;
}

div#head{
	height: 15%;
	background-color: #EEE;
	
	box-shadow: 0 5px 5px #CCC;
	
	padding: 0 15px;

	position: relative;
	margin: 0;
}
div#main{
	padding: 15px;
	text-align: center;
}
span.title{
	font-size: 48pt;
	display: inline-block;
	
	position: absolute;
	top: 50%;
	transform: translateY( -50% );
	-webkit-transform: translateY( -50% );
}

ul#nav{
	float: right;
	margin: 0;
	padding: 0;
	height: 100%;
}
li{
	list-style-type: none;
	vertical-align: middle;
}
ul#nav > li{
	display: inline-block;
	padding: 10px;
	margin: 0;
	height: calc( 100% - 22px );
	width: 150px;
	font-size: 14pt;
	
	cursor: pointer;
	
	color: #BBB;
	
	text-align: center;
	position: relative;
	
	white-space: nowrap;
	
	border-right: 1px solid gray;
	
	transition: all .25s ease-in-out;
}
ul#nav > li:hover{
	color: black;
	font-size: 16pt;
}
ul#nav > li:first-child{
	border-left: 1px solid gray;
}
ul#nav > li > span.text{
	display: inline-block;
	position: absolute;
	top: 50%;
	left: 50%;
	transform: translate( -50%, -50% );
	-webkit-transform: translate( -50%, -50% );
}

/* Dialogs */

div.modal{
	position: fixed;
	z-index: 10;
	top: 0;
	left: 0;
	bottom: 0;
	right: 0;
	
	background-color: rgba( 64, 64, 64, .75 );
	
	visibility: hidden;
}
div.popup{
	background-color: white;
	min-width: 300px;
	min-height: 125px;
	
	position: absolute;
	left: 50%;
	top: 30%;
	transform: translate( -50%, -50% );
	-webkit-transform: translate( -50%, -50% );
	
	padding: 5px;
	
	text-align: center;
	
	visibility: hidden;
	opacity: 0;
	
	transition: all .5s ease-in-out;
	
	box-shadow: 3px 3px 3px rgba( 255, 255, 255, .5 )
}
form{
	margin-top: 15px;
}
table, table > tbody{
	/* *sigh* Yes, this does indeed have to do with the dialogs */
	display: block;
	margin: 0 auto;
}
table > tbody > tr > td{
	display: inline-block;
}
form > table > tbody > tr > td:nth-child(odd){
	text-align: right;
	width: 30%;
}
form > table > tbody > tr > td:nth-child(even){
	text-align: left;
	width: 65%;
}
table > tbody > tr > td:nth-child(odd){
	text-align: right;
	width: 45%;
}
table > tbody > tr > td:nth-child(even){
	text-align: left;
	width: 45%;
}
td{
	border: none;
	padding: 0;
	margin: 10px 0;
}
tr{
	display: block;
}
button.submit{
	position: absolute;
	bottom: 15px;
	left: 50%;
	
	transform: translateX(-50%);
	-webkit-transform: translateX(-50%);
}
div.popup > .title{
	text-align: left;
	margin-bottom: 10px;
}
div.popup[data-dlg="Success"]{
	background: rgb( 144, 238, 144 );
}
div.popup[data-dlg="Failure"]{
	background: rgb( 238, 144, 144 );
}
button.submit{
	position: absolute;
	bottom: 10px;
}
img.closer{
	float: right;
	cursor: pointer;
}

/* Specific styling */
div.popup[data-dlg="Register"]{
	min-height: 200px;
}

div.popup[data-dlg="Login"]{
	min-height: 150px;
}
div#user{
	font-size: 24pt;
}
div#countdown{
	font-size: 24pt;
	margin: 30px 0;
}
div#disp-msg{
	position: relative;
	top: 15px;
	opacity: 0;
	visibility: hidden;
	
	transition: top 1.5s linear,
			opacity 1.5s ease-in-out;
			
	margin-top: 50px;
	font-size: 72pt;
}

hr{
	height: 0;
	border: 1px dashed gray;
}

footer{
	position: absolute;
	bottom: 20px;
	left: 50%;
	
	transform: translateX( -50% );
	-webkit-transform: translateX( -50% );
}

div.row{
	padding: 20px;
	font-size: 24pt;
}
div.question{
	padding: 20px;
	margin: 0 20px;
	border-radius: 5px;
	cursor: pointer;
	display: inline-block;
	
	background-color: rgb( 144, 288, 144 );
	
	user-select: none;
	-webkit-user-select: none;
	-moz-user-select: none;
	-o-user-select: none;
	-ms-user-select: none;
	
	transition: box-shadow .25s ease-in-out;
}
div.question:hover{
	box-shadow: 0 0 20px rgb( 72, 144, 72 );
}

/* UI Styling */
button{
	border-radius: 3px;
	background-color: #DDD;
	border: 2px solid #CCC;
	color: #444;
	padding: 7px;
	font-size: 14pt;
	cursor: pointer;
}
input[type="text"]{
	border-radius: 5px;
	padding: 10px;
	font-size: 14pt;
	border-style: solid;
	border-color: #CCC;
	outline: none;
}
input[type="text"]:focus{
	box-shadow: 0 0 15px lightblue;
	border-color: lightblue;
}